name: SauceDemoAIAutomation

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: maven-${{ runner.os }}-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            maven-${{ runner.os }}-

      - name: Set up Chrome
        uses: browser-actions/setup-chrome@latest

      - name: Set up ChromeDriver
        uses: nanasess/setup-chromedriver@master
        with:
          chromedriver-version: auto

      - name: Verify Chrome installation
        run: google-chrome --version

      - name: Build project
        working-directory: SauceDemoAIAutomation
        run: mvn clean compile

      - name: Run TestNG tests
        working-directory: SauceDemoAIAutomation
        run: mvn test --fail-at-end

      - name: Upload TestNG Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: testng-reports
          path: SauceDemoAIAutomation/target/surefire-reports

      - name: Upload Extent Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: extent-reports
          path: SauceDemoAIAutomation/test-output/ExtentReport.html
